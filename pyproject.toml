[tool.black]
line-length = 120
target-version = ['py311', 'py312', 'py313', 'py314']
extend-exclude = '''
/(
    \.git
  | \.venv
  | \.eggs
  | __pycache__
  | build
  | dist
  | rpmbuild
  | debbuild
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip_gitignore = false
extend_skip_glob = ["build/*", "dist/*", "rpmbuild/*", "debbuild/*", ".venv/*", "__pycache__/*", "*.egg-info/*", ".flatpak-builder/*"]

[tool.flake8]
max-line-length = 120
extend-ignore = ["E203", "W503"]
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    ".eggs",
    "rpmbuild",
    "debbuild"
]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
warn_return_any = true
warn_unused_ignores = true
check_untyped_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_configs = true
show_error_codes = true

[tool.autopep8]
max_line_length = 120
aggressive = 2
recursive = true
in_place = true
exclude = ".git,__pycache__,build,dist,.venv,.eggs,rpmbuild,debbuild"

[tool.pylint.main]
max-line-length = 120
disable = [
    # GTK/GLib Introspection - pylint cannot analyze these correctly (unfixable)
    "E0611",  # no-name-in-module (GTK introspection)
    "E1101",  # no-member (GTK dynamic attributes)
    "E1120",  # no-value-for-parameter (GTK static methods)
    # GTK Required Patterns - intentional, not fixable
    "C0413",  # wrong-import-position (gi.require_version must come first)
    "W0201",  # attribute-defined-outside-init (GTK do_activate pattern)
    "W0613",  # unused-argument (GTK signal handlers require specific signatures)
    # Intentional design patterns
    "C0415",  # import-outside-toplevel (lazy imports for circular dependency avoidance)
    # Docstrings - code is self-documenting, add as needed
    "C0116",  # missing-function-docstring
    # Cross-file checks that are hard to fix
    "R0801",  # duplicate-code (similar code in multiple files - acceptable patterns)
    # Handled by other tools (redundant checks)
    "C0301",  # line-too-long (handled by black/flake8)
    "C0411",  # wrong-import-order (handled by isort)
    "W0611",  # unused-import (handled by flake8)
]
ignore-paths = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    ".eggs",
    "rpmbuild",
    "debbuild",
]
# GTK/GLib introspection creates dynamic members pylint can't see
extension-pkg-whitelist = ["gi"]

[tool.pylint.format]
max-line-length = 120

[tool.pylint.typecheck]
# Generated members from GTK/GLib introspection
generated-members = [
    "gi.repository.*",
    "Gtk.*",
    "Gdk.*",
    "GLib.*",
    "Gio.*",
    "Adw.*",
    "GObject.*",
]

[tool.yamllint]
extends = "default"

[tool.yamllint.rules]
line-length = { max = 120 }
truthy = { check-keys = false }
document-start = "disable"
